# showing node config
```npm
npm config ls -l
```

# view node version
```npm
node --version
```

# Show node installation folder
```npm
npm root -g
npm root
```

# Check package version
```npm
npm list webpack
```
